(function(window) {
    window.__env = window.__env || {};

    // API url
    window.__env.baseurl = 'http://localhost:9001/api/';
    window.__env.orgId = '1';
    window.__env.enableDebug = true;
}(this));