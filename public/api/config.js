// public/admin/js/config.js  (make sure this path is included in index.html before controllers/services)

app.constant('config', {
    baseurl: "http://localhost:9999/1/api/",  // use your Django backend here
    imageurl: "http://localhost:9999/assets/img/", // adjust if needed
    business_id: 40,
    orgId: "1"
});
